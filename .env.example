# App port (only port exposed to the host)
APP_PORT=8080

# Image tag (default: latest)
IMAGE_TAG=latest

# Paperless-ngx (optional)
PAPERLESS_BASE_URL=http://paperless:8000
PAPERLESS_TOKEN=
PAPERLESS_ENABLED=false
